{
  "version": 1.1,
  "atDirectives": [
    { "name": "@theme", "description": "Tailwind v4 — define design tokens (colors, fonts, etc.) consumed at build time." },
    { "name": "@utility", "description": "Tailwind v4 — declare a custom utility class." },
    { "name": "@variant", "description": "Tailwind v4 — declare a custom variant (e.g. hover, dark)." },
    { "name": "@source", "description": "Tailwind v4 — point at extra files to scan for class usage." },
    { "name": "@plugin", "description": "Tailwind v4 — load a Tailwind plugin from a path." },
    { "name": "@config", "description": "Tailwind v4 — reference a tailwind.config.js when migrating from v3." },
    { "name": "@apply", "description": "Tailwind — inline utility classes inside a component rule." },
    { "name": "@layer", "description": "CSS @layer — cascade-layer declaration (also used by Tailwind v3/v4)." }
  ],
  "properties": [
    { "name": "app-region", "description": "Tauri / Electron — mark a region as window-drag (`drag`) or interactive (`no-drag`). Use alongside the canonical `-webkit-app-region`." }
  ]
}
