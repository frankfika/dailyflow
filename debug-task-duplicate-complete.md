[OPEN] task-duplicate-complete

# 问题描述

- 现象：点击任务完成后，任务列表里可能出现重复，尤其是“之前的任务”区域疑似重复展示。
- 目标：确认重复是数据层重复、派生列表重复，还是渲染层重复。
- Debug Server: http://127.0.0.1:7777/event
- Log File: .dbg/trae-debug-log-task-duplicate-complete.ndjson

# 假设

1. 完成任务时，同一条任务被同时保留在原列表，又被追加到历史/已完成派生列表。
2. “之前的任务”使用的筛选条件与主列表重叠，导致同一任务被两个区块同时渲染。
3. 列表 key 不稳定，React 复用了错误节点，造成重复显示假象。
4. 完成任务的乐观更新与后续持久化/同步回写叠加，导致同一任务被插入两次。

# 当前计划

1. 定位任务完成入口、任务列表渲染入口、“之前的任务”数据来源。
2. 只加入调试埋点，复现一次完成任务流程。
3. 用日志排除假设后，再做最小修复。

# 当前证据

- `server/services/fileSystem.ts` 的调试日志显示，`2026-07-01` 当天解析出的任务总数为 18，`duplicateIds = []`，`duplicateTitleStatuses = []`，说明服务端返回前数据本身没有重复。
- 按 `App.tsx` 当前分组规则离线跑了一遍，`duplicateSections = []`，说明同一条任务不会被正常分配到两个区块。
- 本地浏览器复现被一个无关问题阻塞：`__APP_VERSION__ is not defined` 导致 dev 页白屏，所以没有拿到前端点击后的运行日志。
- 扫描最近日期后，发现真实重复数据：
  - `2026-05-30` 中 `准备推特kol` 有 2 条 migrated 记录
  - `2026-05-31` 中 `准备推特kol` 有 2 条 migrated 记录
  - `2026-06-01` 中 `准备推特kol` 有 2 条 migrated 记录
  - 这些行除 `^id` 外内容相同，属于历史迁移脏数据，不是仅仅视觉重复。

# 当前判断

- 假设 4：对当天 `2026-07-01` 不成立，但对历史文件成立一部分。当前读到的数据没有重复；不过历史某些日期文件里确实残留了迁移重复数据。
- 假设 2：对当前分组规则不成立。
- 假设 3：成立。跨区块动画会造成瞬时双显示。
- 新结论：问题有两层。
  1. 前端动画会让任务在“未完成 -> 已完成”移动时短暂看起来重复。
  2. 历史迁移数据里确实存在“同内容、不同 id”的重复行，尤其是带 `↗ migrated:` 的旧任务。

# 已做修复

1. 保留调试埋点，方便后续继续观察。
2. 去掉 `src/components/TaskCard.tsx` 的 `exit` 退场动画，避免同一任务在跨区块移动时短暂双显示。
3. 升级 `server/services/fileSystem.ts` 的自愈去重：
   - 原来只按整行完全相同去重。
   - 现在对带 `↗ migrated:` 的任务行，比较时忽略 `^id`。
   - 这样旧的“迁移重复垃圾数据”会在读取时自动清掉。
4. 增加回归测试，覆盖“同一 migrated 任务仅 id 不同”的去重场景。

# 验证

- 单测：`npm run test -- src/__tests__/components/TaskCard.test.tsx` 通过。
- 待你在真实使用场景里确认：点击未完成任务 -> 观察是否还会短暂出现两条相同任务。
- 单测：`npm run test -- server/services/__tests__/fileSystem.test.ts` 通过。
